version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: wa-postgres
    environment:
      POSTGRES_DB: wa_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
<<<<<<< HEAD
      - ./werneradv20250511.sql:/docker-entrypoint-initdb.d/init.sql
=======
      - ./work_material/werneradv20250511.sql:/docker-entrypoint-initdb.d/init.sql
>>>>>>> a66f3f2 (feat: padronizaÃ§Ã£o de botÃµes e melhorias de UI - PadronizaÃ§Ã£o de estilos de botÃµes (primÃ¡rio azul escuro, secundÃ¡rio e perigo apenas bordas) - CriaÃ§Ã£o de arquivo CSS global para botÃµes (styles/buttons.css) - Melhoria do campo de busca com Ã­cone e estilo moderno - Ajuste na exibiÃ§Ã£o do campo cargo nas matrÃ­culas - EspaÃ§amento entre botÃµes no header de detalhes do cliente)
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: wa-backend
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/wa_db
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: postgres
      SPRING_JPA_HIBERNATE_DDL_AUTO: validate
      SPRING_JPA_SHOW_SQL: "false"
      SERVER_PORT: 8081
    ports:
      - "8081:8081"
    depends_on:
      postgres:
        condition: service_healthy

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        VITE_API_URL: /api
    container_name: wa-frontend
    ports:
      - "5000:80"
    depends_on:
      - backend

volumes:
  postgres_data:

